# Copy to .env.local for local `npm run dev`. Not required for production —
# the deployed page lets you enter/edit the backend URL and token directly
# and remembers them in the browser's localStorage.
NEXT_PUBLIC_BACKEND_URL=wss://your-fly-app.fly.dev
